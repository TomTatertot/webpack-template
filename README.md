This is a template containing webpack, ESLint, and Prettier. It also includes npm commands to build files, run the webpack server, and lint files.

To use this template: 
  1. Create a new repository on Github and select this template under the template drop-down.
  2. Clone the repository
  3. Install npm using 'npm install'
  4. Run 'npm build'
